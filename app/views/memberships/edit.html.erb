<h1>Editing Membership</h1>

<%= render 'form', membership: @membership %>

<%= link_to 'Destroy', @membership, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', admin_decision_path(@membership.decision) %>
