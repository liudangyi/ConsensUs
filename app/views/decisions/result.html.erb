<h1><%= @decision.name %></h1>

<div>
<h4>Group Page</h4>
This is an average of everyone's score.<br>
"Difference between committee and you” indicates the amount of disagreement between you and other voters.<br>
Click on the dots to see others' votes. Hover over voters to see their score and arguments.<br>
Your task is to answer the questions below the visualization.
</div>

<%= render "result_viz" %>

<table class="table">
  <thead>
    <tr>
      <th></th>
      <% @decision.alternatives.each do |alternative| %>
        <th><%= alternative.name %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @decision.criteria.each do |criterium| %>
    <tr>
      <th><%= criterium.name %></th>
      <% @decision.alternatives.each do |alternative| %>
      <td>
        <%= @scores[criterium.id.to_s][alternative.id.to_s]&.map { |s| [s.membership.user.email, s.value] }.to_h %>
      </td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Rate', @decision %>
